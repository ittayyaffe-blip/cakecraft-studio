{
  "name": "cakecraft-studio-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Static HTML/CSS/JS frontend for CakeCraft Studio, served as a standalone Railway service.",
  "scripts": {
    "start": "serve -l tcp://0.0.0.0:$PORT ."
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
